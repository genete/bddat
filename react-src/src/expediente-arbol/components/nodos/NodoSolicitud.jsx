// NodoSolicitud.jsx — nivel solicitud (#500).
// Decoradores específicos (siglas + descripción + fecha presentación) → "formas".
import React from 'react'
import NodoBase from './NodoBase.jsx'

export default function NodoSolicitud({ data }) {
  return <NodoBase data={data} />
}
