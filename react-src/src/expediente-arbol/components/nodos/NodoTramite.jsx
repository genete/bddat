// NodoTramite.jsx — nivel trámite (#500).
// Decoradores específicos (tipo + estado; iteración fuera de v1) → "formas".
import React from 'react'
import NodoBase from './NodoBase.jsx'

export default function NodoTramite({ data }) {
  return <NodoBase data={data} />
}
