// NodoFase.jsx — nivel fase (#500).
// Decoradores específicos (tipo + estado + resultado + plazo agregado) → "formas".
import React from 'react'
import NodoBase from './NodoBase.jsx'

export default function NodoFase({ data }) {
  return <NodoBase data={data} />
}
