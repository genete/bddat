// Inspector.jsx — ESQUELETO S3 (ADR-016 §5).
// En S2 (lectura) no se monta. El detalle lazy del nodo seleccionado, el split
// lectura/edición y la despensa se implementan en S3.
import React from 'react'

export default function Inspector() {
  return null
}
