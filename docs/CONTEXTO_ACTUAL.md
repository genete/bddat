# Contexto actual — BDDAT

> Actualizar al cerrar cada issue. Una entrada por sección, no listas.

---

**Último cerrado:** #387 — eliminar whitelists E-S-F-T y verbos INICIAR/FINALIZAR del motor (ADR-007): DROP 3 tablas whitelist, DROP `doc_consumido_tipo_id`, CHECK constraint `reglas_motor.accion` → solo CREAR|BORRAR, rutas INICIAR eliminadas, FINALIZAR sin motor. Script zombie `reset_maestros_ftt.py` eliminado. Protocolo de análisis de impacto añadido a REGLAS_DESARROLLO.

**Actuales:** #289 — re-scope Context Builders Capa 2: corregir GUIA_CONTEXT_BUILDERS, convención `as_contexto_cb()`, paquete `context_builders/`, CB ContextoConsultaSeparata.

**Próximo:** #391 (ContextoConsultaSeparata) · #392 (ContextoAnalisisDocumental) · #393 (ContextoRecepcionAlegacion) · #394 (ContextoAnalisisAlegaciones) → #386 (semáforos de plazo en vista BC, M3).
