# Contexto actual — BDDAT

> Actualizar al cerrar cada issue. La sección de estado lleva una entrada por línea.
> El detalle y el porqué de cada decisión está en `docs/historial/REVISION_VALIDEZ_ISSUES_MAYO_2026.md`.
> Los issues cerrados están en `git log` — no se duplican aquí.

---

**Último cerrado:** #192 (PR #496) — requisitos documentales por procedimiento (tablas `requisitos_documentales`, `condiciones_requisito`, `documentos_requisito`, helper `evaluar_requisitos`).

**Actuales:** —

**Próximos:** **Bloque UI — Revamping**

## Hoja de ruta

La fase de pre-frontend está completa (#488, #470, #466, #174, #410, #192).

Arranca el bloque de revamping de UI. El primer paso es una **auditoría de la interfaz existente**: qué se poda, qué se refactoriza y qué se construye desde cero. Los issues se abrirán durante la auditoría o se rescatarán issues UI existentes — no están predefinidos.

El resto del backlog (análisis heurístico de PDF, pre-producción) queda en GitHub hasta que se retome.
